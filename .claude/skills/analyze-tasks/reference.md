# 알려진 사실 (분석 시작점)

자세한 근거는 옵시디안 `분석\` 노트에 있다. 여기는 다시 파지 않도록 요점만 모은다. 새로 확정한 것은 이 파일에도 한 줄 더한다.

## 파일 묶음
- 게임 자료 폴더마다 `<폴더>.idx` + `<폴더>.pak`(또는 `Obs00~03.pak`) 과 낱장 파일. 낱장이 우선.
  `.idx` 레코드 36바이트, 파일명·pak 이름은 XOR 0xFF. → `tools/re/pak_extract.py`
- `Bgr/*.bgr` = JPEG 일러스트(640×480). 전투 배경 아님.

## 텍스트 (TXR/Txr.dat)
- 번호 하나로 찾는 전역 표. DLL 로더 `LoadTextData 0x1004a190`: 머리 10바이트 뒤 `(u16 번호, u32 위치, u32 길이)` 레코드. 조회 함수 `0x1004a3f0`(인자 = 번호).
- **주의 (2026-09-18 고침)**: 예전 `parse_txr` 는 번호를 한 칸 밀려 읽었다. 2026-09-17 까지의 노트에 적힌 이름·번호는 한 칸 밀렸을 수 있다(노트 머리 정정 참고). 지금 `tools/extract_character.py parse_txr`·`TxrTable.cs` 는 DLL 과 같다.
- 쓸모 있는 번호대(고친 값): 0~26 메뉴(1 Pannel, 2 Attack, 3 Ability, 4 Item, 5 Rest, 6 Status, 7 Job, 8 System, 9 Info, 10 전직표, 11 능력치, 12 획득한 어빌리티, 13 획득할 수 있는 어빌리티, 14 장비·아이템, 26 Turn),
  34~41 능력치(LP·PSY·DEX·DEP·TP·CTP·STP·SOUL), 156~161 ATK·ACR·RDP·HP·LEVEL·EXP, 162 무속성, 163 상태이상, 166 장착 어빌리티,
  141~155 체질 이름(151 에텔체 … 155 메텔체), 878~882 형(일반·공격·방어·고속·보조), 894~898 계열(사이클론·타키리온·포스트럴·아크로스트·오즈마),
  1375~1380 링 메뉴 설명, 2284~2313 챕터 제목(2284 코어헌터), 이름 예: 463 살라딘, 474 제이슨, 475 죠안.

## 전투
- 전투 상태 `[CBattle+0x4cd0]`, 디스패처 `jmp [eax*4+0x100663f0]`(eax = 상태−1).
  9 메뉴 `0x10068ff0`, 10 일반공격 대상 `0x10069330`, 11 어빌리티 대상 `0x10069a90`, 12 이동 `0x10069c50`.
- 링 메뉴: 생성 `0x1006b810` → 창 생성자 `0x100e0a40`(리소스 0x46d, 반지름 89, 유닛 머리 위 40px),
  키 처리 `0x100e0ec0`(W = 일반공격 0x40a), 결과 나누기 `0x10068c60`.
- 링 그림·효과: UI 리소스 번호 = `Obs` 번호(애니 `0x10025b80`(Obs, 모션)). 아이콘 Attack 0092·Ability 0087·Item 0089·System 0090·Status 0088·Rest 0091(모션 1), 글자 0452, 빛 0105, 틀 0086, 받침 0106.
  항목 `0x100dff30` 상태 4 열기/1 다시열기/3 고름/5 취소 나선 수식, 모션 키 종류 3 = 섞기 방식(17 가산). 소리 65 올림·67 열기·68 고름·69 취소. → `tools/re/obs_ui_dump.py`
- `Btl/NNNN.btl`: 헤더 뒤 0x18 부터 29바이트 유닛 레코드(순번, Chr 코드, x, y, …). 헤더 둘째 워드 → `Map/NNNN.map` 둘째 워드 → `Obt/NNNN.obt`.
- `Obt/NNNN.obt`: `LoadObtFile` `0x10030840`. 40×8 RGB555 띠 타일 + 배치표. 전투 칸 40×32px. → `WarOfGenesis.Assets/ObtMap.cs`
- 코어헌터 훈련장 첫 전투 = `Btl 0045`(아군 죠안 221·살라딘 219·제이슨 62, 적 가이아리더 193), 맵 `Obt 0153`.
- 전투 로딩·턴(TP 게이지)·TP/CTP/STP·Soul: [[분석-전투]]. 도구 `btl_dump.py`, `turn_order.py`, `tp_calc.py`, `soul_costs.py`, `func_calls.py`(함수 호출 나무).
- 배치(ba-6): Btl 캐릭터 파일 8 방향, 9 부대(4 플레이어·3 동맹 AI·0~2 적), 13 레벨보정(+파티 상위 3명 평균, 명부에 없는 유닛만), 15 For.dat 편대, 17~28 AI 꼬리(+0x4dc 이동 방식, +0x4e4 깨어나는 조건 0 즉시/1 동료/2 적 거리/3 틱, +0x4e6 값). 배치칸 C 있으면 상태 2 "캐릭터 배치" 창 `0x10066d00`, 자동배치 상태 1.
- 이동 범위(ba-5): 기준 칸 `+0x4b8/+0x4ba` 에서 칠함, 걷기 TP 는 다음 행동 확정 때 경로비로 뺌. 상태 12 work = 고른 어빌리티.
- 전투 목록(ba-7): `Chp` 장소 레코드(20B) 셋째 워드 <10000 = Btl, 10000~ = Fld, 20000~ 상점. Chp·Fld 스크립트 행동 10 = 전투, 6 = 필드. Btl 이벤트 행동 10 = 이어지는 전투, 6 = 필드. → `tools/re/battle_list.py`, [[분석-전투목록]].
- 범위·자세·상태이상(fg-6): 거리 = 4×맨해튼 + 높이항(`0x100daca0`), 한 칸 = 4. 사거리 최소 = 파일값×4−3, 최대 = ×4, 효과 범위 최대 = `+0x1a`×4.
  모양 1 마름모·2 십자·3 부채꼴·4 화면 전체·5 직선·6 폭3 줄·7 폭5 줄·8 대각선·9 삼각형(표 `0x10075b7c`, 사거리는 네 방향 합침).
  대상 거르기 `+0x13`(사거리)·`+0x1e`(효과): 0·2 자기, 1 적, 3·6 아무 칸, 4 아군, 5 아무 유닛, 7 빈 칸, 8 오브젝트.
  자세 `+0x4d4`: work 516 방어 = 1, 515 회피 = 2, 다음 차례에 풀림. 상태이상 = 유닛 슬롯 3칸(`+0x4bf`/`+0x4c2`), 뜻은 `Dat/Sta.dat` 설명 TXR,
  지속 시간 없음(44·45·46 이 칸 비우기 = 큐어), 5·6 은 틱마다 3%. → [[분석-전투]] 끝 절, `tools/re/work_area.py`.
- 스탯 쓰임(an-4): PSY 는 공격력만, DEX 는 이동비·명중·자세, 명중은 현재 TP 차이도 씀, ACR 은 표시·AI 만(판정 안 씀), 레벨은 경험치·적 레벨 맞추기·Lev.dat 성장만. [[분석-캐릭터]] an-4.

## 캐릭터
- `Chr/NNNN.chr` 90바이트, `CChr.Load` `0x10031530`. 배치표는 `tools/re/chr_dump.py` 머리말.
  +2 이름, +8 sprite, +10 face, +12 칭호·소속 텍스트, **+14 체질**(0 무속성·1 에텔(사이클론)·2 멘탈(타키리온)·3 아스트럴(포스트럴)·4 코절(아크로스트)·5 메텔(오즈마), 255 NPC — 죠안 화면 "오즈마"로 확인),
  **+15 직업 = Job.dat 번호**, +42 장비 6칸(무기·갑옷·아뮬렛·반지·벨트·신발), +56 (어빌리티, 레벨)×8.
- `Dat/Dep.dat`: `LoadSystemData`, 전역 `0x101b6860`, 16 레코드 = 직업 묶음(사이클론·타키리온·포스트럴·아크로스트·오즈마 × 단계), 각 레코드에 직업 번호들.
- `Dat/Job.dat`: `LoadJobData`, 전역 `0x101b6874`. 파일 레코드 67바이트, 53 오프셋에 체질별 직업 이름 TXR 6칸.
- `Dat/Itm.dat`: `LoadItemData`, 전역 `0x101b687c`. 파일 레코드 48바이트(번호, 이름, 가격 u32, **종류 u8** …). 종류: 0 VES, 1 머리, 2 갑옷, 3 신발, 4 벨트, 5 반지, 6 아뮬렛, 7 캡슐, 8 리본, 9 요요, 10 사진기, 11 크로, 12 쌍권총, 13 시가, 14 일반검, 15 대검, 16 사용, 17 포이즌 포트, 19 적용 무기.
- 패널: Status 쪽 캐릭터 패널 `0x10034050`(칭호·계열·직업·HP·LEVEL…), 전투 중 유닛 패널 `0x100d3d70`(HP·LEVEL·RDP·STP·DEP·SOUL·ATK·ACR). 텍스트를 상수로 부르는 곳 목록은 전체 디스어셈블에서 `call 0x1004a3f0` 직전 push 를 모으면 나온다.
- Status 편집(구현 st-1~3, [[분석-캐릭터]] 끝 절): Status 창은 전투 링 메뉴에서만(`0x100e0500`), 값 바꾸기는 `0x100e123c`(0x272f/0x2730 레벨·배우기, 0x2733 장착 어빌리티, 0x2734 장비), TP 비용 없음.
  CChr 932바이트: +0x30 쓸 수 있는 EXP, +0x4a 무기 종류(.chr 39), +0x4c 장비, +0x7a 어빌리티별 레벨 u8×200(0xff 없음), +0x142 장착 어빌리티×3.
  .abi 파일 12 = 종류(1 전투, 3 패시브, 0 수련, 2 링크), 6/8·9/11 = 선행 어빌리티·레벨. Dep.dat 파일 4 = 장착 칸 수. Job.dat 파일 31 = 배울 수 있는 어빌리티×11.
  세이브 `G3P_II%02d.sav` = XOR 0xFF, 머리 22 + 2000 + (u16 N, CChr 932×N) + 파티 4272×3(+0x108 아이템 수, +0x110 (아이템, 개수)). → `tools/re/sav_dump.py`
- 다른 dat 로더: `LoadItemData`(Itm.dat), `LoadAbilityData`(0000~0012.att), `LoadValueData`(Num.dat), `LoadStData`(Sta.dat), For/Dmg/Lev.dat.

## 몸짓 (Obs)
- `Obs/NNNN.obs`: 몸짓벌(subentry) 여러 개 × 장(slot). 둘째 벌부터는 앞 벌 끝에서 찾는다. → `WarOfGenesis.Assets/ObsSprite.cs`
- **동작표는 Obs 안에 있다**: 벌 색인표 뒤 `u16 모션 수, u16 최대번호, (u16 번호, u32 위치)×m`. 모션 = 시간줄 키(26바이트: 종류·시작틱·길이·인자10; 종류0 그림(벌,장), 2 다른 Obs 모션). 로더 `0x1002fa00`.
- 모션 번호 = 동작×3 + 방향(0 뒤, 1 옆, 2 앞; 3 은 옆). `SetAction 0x10072820`. 0 서기, 1 걷기. 걷기 컷 손표 `WalkCycles.cs` 는 이걸로 대체 가능.
- 스킬: `Dat/*.att` = work 레코드(62바이트, 로더 문자열은 LoadWorkData), `Abi/*.abi` = 어빌리티(26바이트, 레벨→work). `.chr` +19 = 기본공격 work. 실행 `0x1007ca40`: +0x3f 준비 동작 → work 번호 switch → 핸들러(동작 상수). → `tools/re/skill_motion.py`

## 실행 환경
- 실제 게임: `Desktop\ETC\DGGL\Games\G3P2DVD_Win95_230710` (DOSBox + Win95 이미지). 메모리 읽기 `tools/find_current_battle.py`.

## 스킬·체질 (2026-09-18, [[분석-스킬]] [[분석-체질]])
- Abi 레코드: +0x08/+0x0a 선행1·레벨(처음 배우면 선행1 지움 `0x10032a80`), +0x0c/+0x0e 선행2, **+0x0f 분류**(0 비전투·1 전투·2 군단기·3 패시브), +0x16/+0x18/+0x1a 아이콘(대상쪽·대상수·攻回異), +0x1c 설명 TXR. 배울 목록 = Job `+0x22` 11칸(`0x100324e0`). 도구 `tools/re/skill_list.py`(--levels, --csv, --jobs).
- `Dat/For.dat` = 군단 표(전역 `0x101b6840`), 군단기(0019.abi) 조건. `Dat/Dep.dat` 레코드: 이름, **+6 장착 어빌리티 칸 수**, 직업 7칸 — 계열 5×단계 3(1 제 체질 5형, 2 타 체질용 5형, 3 최고 발전형 2).
- 형(TXR 878 일반형·879 공격형·880 방어형·881 고속형·882 보조형) = Dep 레코드 안 직업 자리 0~4. Job `+4` 필요 레벨(1/4/8/10/12, 30…, 60), `+6/+8` 필요 어빌리티.
- 체질이 코드에서 바꾸는 것은 비용 덧붙임(Num 44~58, work +0x2e)과 직업 이름(Job +0x38[체질])뿐. 피해·성장·배울 목록은 직업만 본다.

## 소리 (2026-09-18, [[분석-사운드]] fa-4·fa-5)
- 효과음 `0x10028850(번호,x,y)` → `Snd\%04d.Snd`. 전투 공격·어빌리티 소리는 **Obs 모션 키 종류 1(인자0 = Snd 번호)** — 유닛·이펙트 공용 키 처리 `0x100e5410`, 종류 2 자식 모션 소리도 울림. 도구 `tools/re/obs_sounds.py`.
- 코드 소리: 죽음 106, 레벨업 107. `Dat/Dmg.dat`(14B: 번호, 비명×2, 부르기×4) ← `.chr` +6 = 음성 묶음. 맞음(피해>0) 비명, 차례(TP 가득) 부르기.
- 음악: Btl 머리 word 8 → `BGM\%04d.bgm`, 곡 전체 반복(`0x100252a0(번호,1,1)`), 음량 `0x10025320(%)`. 승리 3392·패배 55 한 번. 스크립트 명령 512 곡 바꾸기, 513 멈춤.

## 맞는 효과 (2026-09-18, [[분석-전투]] fg-10)
- 반응표 두 개(`0x1007a344` 유닛 공격 / `0x1007a32c` 오브젝트), 결과종류 2 맞음 → `SetAction2(기준 6 = 동작 2, 제 방향, 반복 1)` + Obs **73** 모션 rand%3 + 비명. 3 빗나감 → 아무것도 안 함. 1 회복 → 표를 안 탐.
- 동작 2 모션(6·7·8)은 캐릭터 공통 15틱 정지 컷 + 키 4(물들이기 방식 3, 세기 5→15→30→15→5 = 흰 번쩍) + 키 7(x ±1 떨림 t4~t11). 칸 밀림 없음.
- 이펙트 붙이기 `0x100d29d0(Obs, 모션, x, y, z, 방향, 주인)`. 자리 갱신 `0x100ea910`: 화면 = (월드x, 월드y×32/40 − 월드z×12/20). 불꽃은 (0,0,30) = 18px 위, 가산(키 3 인자 17).
- 숫자·글자 창: 피해 `0x100d22e0`(빨강 "HP n", (−30,0,40)), 회복 `0x100d27b0`(노랑, 옛 HP→새 HP 세어 올림, 안 떠오름), Miss `0x100d2650`(연두, TXR 42). 떠오름 `0x100d2610`: z += 40/나이, 20틱에 사라짐. GDI TextOut + 검정 1px 테두리.
- 치명타 `0x1002e8f0(9,16,1,0)` = 한 틱 전역 물들이기(방식 9 = 픽셀/2 = 절반 밝기). 카메라 흔들림 아님. 물들이기 방식 1~7 = `(k·픽셀 + (31−k)·세기)/31`, k = 27,23,19,15,11,7,3.
- 죽음 0x2717 도 **동작 2**(기준 6, 반복 1000) + Snd 106, 21틱.
- 모션 키 뜻 확정(`0x100276e0`): 0 그림, 1 소리, 3 섞기(+0x13), **4 물들이기(+0x14 방식, +0x15 세기)**, 5 +0x30, **7 자리 덮어쓰기(+0x32 x, +0x34 y)**. 유닛 `+0x7a` = 모션 반복 횟수, 다 쓰면 서기로.

## 군단 (2026-09-18, [[분석-군단]])
- `Dat/For.dat` 59바이트 × 79(번호 0~80), 로더 `0x1004ce40` → 전역 `0x101b6840`(64B): 이름 TXR, +6 부하 Chr 6칸, +0x12 진형 0~5(TXR 813+진형, 칸 표 `0x10164838`),
  +0x14~+0x1c 부하 LP·TP·PSY·DEX·DEP 보정(LP·PSY·DEP 만 읽음: 대장 세력 × 값/100), +0x1e (군단기, 필요 세력, 대장 Chr)×5, +0x3c 설명 TXR.
- CChr `+0x1c` = 배속 군단(로드 때 0), `+0x148` u16×300 = 군단 세력(로드 때 1000). 파티 `+0x910` 가짓수 + `+0x914` (군단, 개수)×32.
- 유닛: +0x4ea 군단, +0x4ef 대장, +0x4f0 부하, +0x4ec 보정 받음, +0x4fc 부하 수, +0x500/+0x504/+0x508/+0x50c/+0x510 목록. 붙이기 `0x10072ef0`, 떼기 `0x10072fd0`, 대장 사망 `0x100716c0`(세력 ×0.6, 부대 4→3).
- 부하는 차례를 안 받고(`0x1006af1a`) 대장 행동 뒤 상태 **15** `0x10069ff0` 에서 함께 움직임(휴식 `0x1005f610`, 이동 `0x1005f670`, 이동+휴식 `0x1005f870`, 공격 `0x1005fa90`; work `+0x41`=0 이면 대장만).
- 군단기(0019.abi, 분류 2) 조건 `0x10032760`, 메뉴 켜기 `0x100d58c2`(대장 && 부하 수 = For 부하 칸 수). 스크립트 행동 713 군단 얻기 / 714·715 세력(자료에는 없음). 도구 `tools/re/legion_dump.py`.

## Status 창 수치 (2026-09-18, [[분석-캐릭터]] an-2)
- 능력치 칸 그리기 `0x100d4740`(줄 간격 (높이−116)/25+4), 상태이상 칸 `0x100d5448`(유닛 +0x4bf[i], Sta.dat `0x101b6858`).
- 화면 HP = 내부 × (갑옷 + Num6)/Num6, ATK = ((무기공격+Num1)×PSY/Num25)×(SOUL+Num2)×Num42/Num85, ACR = (2CTP+최대TP+현재TP)/Num9 + DEX/Num8, RDP = DEP×(1+(1−HP/최대)²×Num39/100).
- `.chr` 39(CChr+0x4a) = 낄 수 있는 무기 종류(장비 목록 거르기 전용), `.chr` 40(CChr+0x48) = "WEAPON ○○" 띠 그림 번호. `.chr` 4(이름2)는 텍스트로 쓰는 곳 없음.
- 도구 `tools/re/status_calc.py` — 세이브로 Status 숫자 재현(죠안 17개 값 일치).

## 모세스(챕터) 화면·레벨업 알림 (2026-09-18, [[분석-모세스]] [[분석-캐릭터]])
- **모세스 화면 = 챕터 장면(장면 4, `0x100f56c0`)**. 전투가 이기고 끝나면(`[CBattle+0xa4]`=1) 장면 4 로 돌아간다(결과 jmp 표 `0x10061ecc`: 4 → 장면 6, 5 → 다음 전투, 6 → 필드, 7 → 같은 전투 다시).
  화면 창 0x2f1c(`0x100f66c0`, 전역 `0x101e7c7c`), 상태 저장 `0x101b6898`(+4 챕터 · +6 항행 단계 · +8 성계 · +a 행성 · +c 페이지 · +0x194 소리 갈래).
  메뉴 6개(아이콘 Obs 291 모션 4·6·8·10·12·23, 창틀 Obs 246, 자리 (50/105/160,400)·(25/80/135,435), 설명 TXR 485~490), 처리기 `0x100fea70`, 지도 칸 = 명령 10~17.
  전환 큐 `+0x2e0c`: 양수 UI 효과 / 10000+ 대기 틱 / 20000+ 소리 / **음수 = `Mov\NNNN.mov`**(0011 부팅, 0012 성계, 0015). → `tools/re/mov_frames.py`(PyAV 로 Bink 영상 프레임 PNG).
  **전투 결과·경험치 정산 화면은 없다**(아이템·GP 획득 창은 전투 안 `0x1006e9a0`·`0x1006ea40`).
- **Chp 머리 셋째 워드(`+0x178`) = 챕터 BGM 번호**(`0x100fe6ac`) — [[분석-전투목록]]의 "갈래 19/22/51" 정정.
- 레벨업 알림(fg-9): 전투 상태 18(죽은 유닛) → 19(물체) → **21 `0x100681a0`** → 4. 카메라 이동 → `LevelUp 0x100318d0` → 창(제목 TXR 1090 `Level Up`, 본문 TXR 1091 + `$nLP가 %d 상승하였습니다.` 류 `0x1016ef38~0x1016efa8`) + 소리 Snd 107, BGM 소리 40 → 끝나면 100. 창 `0x10034540(…, 0x4b5, …, 180, 1, …)` 은 180틱 뒤 자동 닫힘(`+0x108` 카운트다운), 닫히면 다음 사람. HP·TP 회복 없음, 난수 없음.

## 전투 시스템 메뉴·세이브·종료 (2026-09-18, [[분석-시스템메뉴]])
- 링 SYSTEM(결과 `0x410`) → 창 `0x100e3700`(창번호 **0x49e**, 주인 CBattle), 전투 상태 **25** `0x1006b290`. 1열 6행 목록, 칸 190×37, 여백 12, 화면 가운데.
  항목(위→아래) MISSION `0x4c6` · RESTART `0x4bc` · LOAD `0x4be` · SAVE `0x4bd` · VOLUME `0x4a0` · EXIT GAME `0x4bf`.
  글자는 TXR 이 아니라 **`Obs 0894`** 그림(모션 11 MISSION·3 RESTART·1 LOAD·0 SAVE·2 VOLUME·4 EXIT GAME; 장7/8 = 음량 막대 칸, 벌1 = 승/패 아이콘).
  `ds:0x101b5de0`(네트워크 대전) 이면 앞 넷이 꺼짐. 마우스 전용, 단추 소리 없음. 저장 성공 `Snd 579`.
  옵션은 음량 창 `0x272a`(220×160, BGM/SE 20칸 막대 = 5n+5, 배경음악 체크)뿐 — 전투 속도·메시지 속도 없음.
- 세이브 `G3P_II%02d.sav` 머리 22B **확정**: u32 판 `0x1017037c`(7) · u32 플레이시간 ms `0x101737a4` · u32 장면 vt+0x1c(안 씀) ·
  u32 **장면 번호**(1 전투·4 챕터·7 연대표) · u32 장면 인자(전투 번호) · u16 CD `0x101638e4`. 본문 `0x1004d9d0`.
  슬롯: Save 20칸(0~19) / Load 21칸(0~20), **20 = 자동 저장 전용**(상태 22 `0x1006acc0` 에서 `0x10112b50` = SaveGame(20)).
  **전투 상태(자리·HP·TP·차례)는 저장 안 함**. 전역 파티는 전투 끝에만 갱신(`0x10064800`/`0x10064920`) → 전투 중 저장 = 전투 시작 직전 상태.
- 장면표 `0x10173430`: 1 전투 `0x10061610` · 2 네트워크 · 3 필드 `0x100eb9e0` · 4 챕터 `0x100f56c0` · **6 타이틀** `0x10105260`(BGM 21, 창 0xfa1) · 7 연대표.
  본 루프 `0x10112918`: `[0x10173424]`=0 게임 끝 / ≥20 슬롯(=값−20) 불러오기 / 그 밖 장면 번호.
- 전투 결과 `[CBattle+0xa4]` → `0x10061ecc`(index = 결과−2): 1 → 장면 4, 5 → 다음 전투, 6 → 필드, **7 Restart → 같은 전투**, **8 Load**, **9 Exit → 장면 6 타이틀**(바탕화면 종료 아님).
  소멸자 `0x100646d0` 는 결과 9·7·2 면 `0x10064920`(전투 파티 → 전역 되돌리기)을 **건너뛴다** → 그 전투 EXP·장비 변경 전부 버림.
